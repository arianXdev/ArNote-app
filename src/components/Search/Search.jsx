import styles from "./Search.module.css";

const Search = () => {
	return (
		<div className={styles.Search}>
			<div className="container">Search Component</div>
		</div>
	);
};

export default Search;
